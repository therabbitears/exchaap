<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Xaminals.ViewModels.Settings"
             mc:Ignorable="d"
             x:Class="Xaminals.Views.Settings.Categories"
             Title="{Binding Title}">
    <ContentPage.BindingContext>
        <local:SettingPageCategoriesViewModel />
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout StyleClass="parentContainer">
            <Label Text="Categories"/>
            <CollectionView ItemsSource="{Binding Categories}" ItemTemplate="{StaticResource SelectionStackTemplate}" />
            <Button Text="Update categories" Command="{Binding SaveCategoriesInfoCommand}" StyleClass="primaryButton" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>