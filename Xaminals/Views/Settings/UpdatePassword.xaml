<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Xaminals.Views.Settings.ViewModels"
             mc:Ignorable="d"
             x:Class="Xaminals.Views.Settings.UpdatePassword"
             Title="{Binding Title}">
    <ContentPage.BindingContext>
        <local:ChangePasswordViewModel />
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout StyleClass="parentContainer">
            <Entry Text="{Binding CurrentPassword.Value}" Placeholder="Current password" IsPassword="True" />
            <Label Text="{Binding CurrentPassword.Errors[0]}" StyleClass="validationError"/>
            
            <Entry Text="{Binding Password.Value}" Placeholder="Password" IsPassword="True" />
            <Label Text="{Binding Password.Errors[0]}" StyleClass="validationError"/>

            <Entry Text="{Binding ConfirmPassword.Value}" Placeholder="Confirm password" IsPassword="True" />
            <Label Text="{Binding ConfirmPassword.Errors[0]}" StyleClass="validationError"/>

            <Button Text="Update password" Command="{Binding UpdatePasswordCommand}"  StyleClass="primaryButton" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>